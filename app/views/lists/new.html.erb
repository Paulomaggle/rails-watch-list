<h1>Build your new list</h1>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>
<div class="btn btn-outline-primary">
  <%= link_to "go back to all lists", lists_path %>
</div>
